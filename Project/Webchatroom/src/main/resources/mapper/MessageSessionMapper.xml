<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.webchatroom.mapper.MessageSessionMapper">

    <!-- 获取用户的所有会话ID（仅私聊） -->
    <select id="getSessionIdsByUserId" resultType="java.lang.Integer">
        select sessionId from message_session
        where sessionId in (
        select sessionId from message_session_user where userId = #{userId}
        )
        order by lastTime desc
    </select>

    <!-- 获取会话中的好友（排除自己） -->
    <select id="getFriendsBySessionId" resultType="com.example.webchatroom.model.Friend">
        select userId as friendId, username as friendName
        from user
        where userId in (
        select userId from message_session_user
        where sessionId = #{sessionId} and userId != #{selfUserId}
        )
    </select>

    <!-- 获取会话的最后一条消息 -->
    <select id="getLastMessageBySessionId" resultType="java.lang.String">
        select content
        from message
        where sessionId = #{sessionId}
        order by postTime desc
        limit 1
    </select>

    <!-- 创建新会话 -->
    <insert id="addMessageSession" useGeneratedKeys="true" keyProperty="sessionId">
        insert into message_session values(null, now())
    </insert>

    <!-- 添加会话用户关联 -->
    <insert id="addMessageSessionUser">
        insert into message_session_user values(#{sessionId}, #{userId})
    </insert>

    <!-- 更新会话最后访问时间 -->
    <update id="updateLastTime">
        update message_session set lastTime = now() where sessionId = #{sessionId}
    </update>

</mapper>